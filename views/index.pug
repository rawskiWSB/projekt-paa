block content
  
  form(action="/tasks/add", method="post")
    label(for="title")
      | Tytuł
    br
    input(type="text" id="title" name="title")
    br
    label(for="info")
      | Opis
    br  
    textarea(id="info" cols="30" rows="4")
    br
    input(type="submit", value="Dodaj")
  table(style={border: '1px solid black'})
    tr(style={border: '1px solid black'})
      th(scope= "col" style={border: '1px solid black'}) Tytuł
      th(scope= "col") Opis
      th(scope= "col") Status
      th(scope= "col") Akcje  
    each task in tasks
    tr
      td= task.title
      td= task.info
